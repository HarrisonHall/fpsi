name: "SQLite Database"

# DEBUG decides to print information
DEBUG: true

#aggregations_per_second: 4

plugins:
  sqlite-db:
    path: "sqlite-db.so"
    config:
      db_path: "/tmp/fpsi.sqlite"
      #db_path: ":memory:"
      foo: "bar"
  counter:
    path: "../counter/counter.so"
    config:
      foo: "bar"
