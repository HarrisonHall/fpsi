name: "echoecho server"

DEBUG: true

plugins:
  zeromq-socket:
    path: "include/fpsi/plugins/zeromq/zeromq-socket.so"
    config:
      is_server: true
      port: 4001
      ip: "*"
  echoecho:
    path: "echoecho.so"
    config: {}
