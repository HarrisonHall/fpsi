name: "echoecho client"

DEBUG: true

plugins:
  zeromq-socket:
    path: "include/fpsi/plugins/zeromq/zeromq-socket.so"
    config:
      is_client: true
      port: 4001
      ip: "localhost"
  echoecho:
    path: "echoecho.so"
    config: {}
